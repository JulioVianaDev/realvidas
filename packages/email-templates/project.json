{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "@realvidas/email-templates",
    "sourceRoot": "packages/email-templates/src",
    "projectType": "library",
    "tags": ["scope:shared", "lang:ts"],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run build",
                "cwd": "packages/email-templates"
            },
            "inputs": ["production"],
            "outputs": ["{projectRoot}/dist"],
            "cache": true
        }
    },
    "implicitDependencies": ["@realvidas/types"]
}
