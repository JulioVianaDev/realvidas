Light/dark theme switch. Sets `data-theme` on `<html>` and persists to `localStorage` ("rv-theme"). Place one in any header.

```jsx
<ThemeToggle />
```

Shows a moon in light mode, a sun in dark mode. Sizes: `sm` `md` `lg`.
