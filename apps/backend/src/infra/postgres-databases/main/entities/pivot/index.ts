/**
 * Pivot entities for junction tables.
 * Not included in mainConfig.entities by default — add explicitly when you need them.
 */
export { UserTenantPivotEntity } from './user-tenant.pivot.entity';
